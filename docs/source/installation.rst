Installation
============

The most recent release can be installed from `PyPI
<https://pypi.org/project/tess_downloader>`_ with uv:

.. code-block:: console

    $ uv pip install tess_downloader

or with pip:

.. code-block:: console

    $ python3 -m pip install tess_downloader

Installing from git
-------------------

The most recent code and data can be installed directly from GitHub with uv:

.. code-block:: console

    $ uv pip install git+https://github.com/cthoyt/tess-downloader.git

or with pip:

.. code-block:: console

    $ python3 -m pip install git+https://github.com/cthoyt/tess-downloader.git

Installing for development
--------------------------

To install in development mode with uv:

.. code-block:: console

    $ git clone git+https://github.com/cthoyt/tess-downloader.git
    $ cd tess-downloader
    $ uv pip install -e .

or with pip:

.. code-block:: console

    $ python3 -m pip install -e .
